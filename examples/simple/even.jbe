
// remeber all functions are variables, however, main does not need to be defined
// main is already predefined as (func<int, ptr<string>, ptr<string> -> int>). 
// all functions return 0 / null for all applicable return values when no return statement is present

main = {
    int x = 17; 

    if (x % 2)
        print("x is odd");
    else
        print("x is even");
}

